services:
  frontend:
    build:
      context: .
      target: dev
    ports:
      - "3001:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    env_file:
      - .env.production
    environment:
      - WATCHPACK_POLLING=true
